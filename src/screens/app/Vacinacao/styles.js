import styled from 'styled-components/native'

export const VaccineScroll = styled.ScrollView.attrs({
    contentContainerStyle: {
        flexDirection: 'row',
        flexWrap: 'wrap',
        justifyContent: 'space-between',
    },
})``
